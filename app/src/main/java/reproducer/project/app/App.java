/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package reproducer.project.app;

import reproducer.project.list.LinkedList;

import static reproducer.project.utilities.StringUtils.join;
import static reproducer.project.utilities.StringUtils.split;
import static reproducer.project.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
